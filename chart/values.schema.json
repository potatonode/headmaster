{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "headscaleInstances": {
      "additionalProperties": {
        "description": "A headscale control-plane instance managed by headmaster.\n\nCreating a `HeadscaleInstance` causes the operator to deploy a headscale StatefulSet,\na Service, and a ConfigMap containing the rendered headscale configuration. Child\nresources are owned by the instance and are garbage-collected when it is deleted.",
        "properties": {
          "dnsBaseDomain": {
            "description": "Base domain for Magic DNS node hostnames (e.g. `ts.example.com`).\n\nNodes will be reachable at `<hostname>.<dns_base_domain>` via the tailnet.",
            "minLength": 1,
            "type": "string"
          },
          "extraConfig": {
            "default": {},
            "description": "Arbitrary extra configuration merged into the headscale config file.\n\nThe admission webhook rejects entries that would collide with operator-managed keys:\ntop-level `server_url`, `listen_addr`, `grpc_listen_addr`, `grpc_allow_insecure`,\n`metrics_listen_addr`, `unix_socket`, `unix_socket_permission`, `noise`, `database`,\n`policy`, and the `dns.magic_dns` / `dns.base_domain` subkeys. Other `dns.*` keys\n(e.g. `nameservers`, `split_dns`, `extra_records`) are deep-merged into the operator\ndefaults. Omit this field to use defaults only.",
            "x-kubernetes-preserve-unknown-fields": true
          },
          "labels": {
            "additionalProperties": {
              "type": "string"
            },
            "default": {},
            "description": "Extra labels applied to all child resources (ConfigMap, Service, StatefulSet).\nOperator-managed labels (`app.kubernetes.io/name`, `instance`, `managed-by`) always win.",
            "type": "object"
          },
          "policy": {
            "anyOf": [
              {
                "required": [
                  "inline"
                ]
              }
            ],
            "description": "Headscale access-control policy applied to this instance.\nWhen omitted, headscale allows all traffic by default.",
            "nullable": true,
            "properties": {
              "inline": {
                "description": "The policy document as a HuJSON or JSON string.\nOmit the entire `policy` field to use headscale's default allow-all mode.",
                "type": "string"
              }
            },
            "type": "object"
          },
          "resources": {
            "description": "Resource requests and limits for the headscale container.\nDefaults to `50m`/`64Mi` requests and `500m`/`512Mi` limits when omitted.\nThe entire block is replaced when provided — partial overrides (e.g. only\n`requests` without `limits`) leave the unset half empty, not defaulted.",
            "nullable": true,
            "type": "object",
            "x-kubernetes-preserve-unknown-fields": true
          },
          "scim": {
            "description": "SCIM 2.0 server configuration. When set, the operator deploys a SCIM sidecar\nand validates that `spec.policy.inline` contains no `groups` key.",
            "nullable": true,
            "properties": {
              "expireNodesOnChange": {
                "default": false,
                "description": "When true, expire all of a user's headscale nodes when the identifier\nused by `policyUserKey` changes (e.g. email change in `email` mode,\nusername rename in `username` mode). Forces immediate OIDC re-authentication.\nNot needed for `external_id` mode — the ProviderIdentifier never changes.\nDefault: false.",
                "type": "boolean"
              },
              "oidcIssuer": {
                "description": "OIDC issuer URL (e.g. `\"https://pocket-id.example.com\"`).\nRequired when `policyUserKey` is `\"external_id\"`.",
                "nullable": true,
                "type": "string"
              },
              "policyUserKey": {
                "description": "Controls which identifier is written into headscale policy group entries\nand used to locate a user's headscale nodes for session management.\nValues: `\"email\"` (default), `\"username\"`, `\"external_id\"`.\nUse `\"external_id\"` with Pocket ID or Authentik for the most stable\nidentifier (maps to the OIDC `sub` claim / headscale `ProviderIdentifier`).",
                "enum": [
                  "email",
                  "username",
                  "external_id"
                ],
                "nullable": true,
                "type": "string"
              },
              "storage": {
                "default": {
                  "size": "1Gi",
                  "storageClass": null
                },
                "description": "Persistent storage for the external-ID mapping file.",
                "properties": {
                  "size": {
                    "default": "1Gi",
                    "description": "PVC storage request (e.g. `1Gi`).",
                    "minLength": 1,
                    "type": "string"
                  },
                  "storageClass": {
                    "description": "StorageClass to use. Omit to use the cluster default.",
                    "minLength": 1,
                    "nullable": true,
                    "type": "string"
                  }
                },
                "type": "object"
              }
            },
            "type": "object",
            "x-kubernetes-validations": [
              {
                "message": "oidcIssuer is required when policyUserKey is 'external_id'",
                "rule": "!has(self.policyUserKey) || self.policyUserKey != 'external_id' || has(self.oidcIssuer)"
              }
            ]
          },
          "serverUrl": {
            "description": "Publicly-reachable URL of this headscale server, used for Magic DNS and DERP\nconfiguration (e.g. `https://headscale.example.com`).",
            "minLength": 1,
            "type": "string"
          },
          "storage": {
            "description": "Persistent storage for the headscale SQLite database.",
            "properties": {
              "size": {
                "default": "1Gi",
                "description": "PVC storage request (e.g. `1Gi`).",
                "minLength": 1,
                "type": "string"
              },
              "storageClass": {
                "description": "StorageClass to use. Omit to use the cluster default.",
                "minLength": 1,
                "nullable": true,
                "type": "string"
              }
            },
            "type": "object"
          },
          "watchedNamespaces": {
            "description": "Namespaces from which `headmaster` Ingresses may reference this instance.\nEmpty list (the default) allows Ingresses from any namespace.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "dnsBaseDomain",
          "serverUrl",
          "storage"
        ],
        "type": "object"
      },
      "type": "object"
    },
    "headscaleImage": {
      "additionalProperties": false,
      "description": "Container image for the headscale server deployed by this operator.",
      "properties": {
        "repository": {
          "minLength": 1,
          "type": "string"
        },
        "tag": {
          "minLength": 1,
          "type": "string"
        }
      },
      "type": "object"
    },
    "proxyImage": {
      "additionalProperties": false,
      "description": "Container image for the Tailscale proxy sidecar deployed per Ingress.",
      "properties": {
        "repository": {
          "minLength": 1,
          "type": "string"
        },
        "tag": {
          "minLength": 1,
          "type": "string"
        }
      },
      "type": "object"
    },
    "image": {
      "additionalProperties": false,
      "description": "Container image for the headmaster operator.",
      "properties": {
        "repository": {
          "minLength": 1,
          "type": "string"
        },
        "tag": {
          "description": "Defaults to the chart's appVersion when empty.",
          "type": "string"
        },
        "pullPolicy": {
          "enum": [
            "Always",
            "IfNotPresent",
            "Never"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "ingress": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Set to false to disable the Ingress controller. Useful when headmaster is used solely to manage HeadscaleInstance lifecycles.",
          "type": "boolean"
        },
        "watchNamespaces": {
          "description": "Namespaces to watch for Ingress objects. Empty list means all namespaces.",
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "webhook": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Enable the admission webhook for HeadscaleInstance config validation. Requires cert-manager.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "rustLog": {
      "description": "Log filter directive passed as `RUST_LOG` (e.g. `info`, `operator=debug,info`).",
      "minLength": 1,
      "type": "string"
    },
    "nodeSelector": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Node labels for pod scheduling. See the [Kubernetes docs](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector).",
      "type": "object"
    },
    "tolerations": {
      "description": "Pod tolerations for tainted nodes. See the [Kubernetes docs](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).",
      "items": {
        "type": "object"
      },
      "type": "array"
    },
    "affinity": {
      "description": "Pod affinity and anti-affinity rules. See the [Kubernetes docs](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#affinity-and-anti-affinity).",
      "type": "object"
    },
    "podAnnotations": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Annotations added to the headmaster operator pod.",
      "type": "object"
    },
    "resources": {
      "description": "Resource requests and limits for the headmaster operator container. See the [Kubernetes API reference](https://kubernetes.io/docs/reference/kubernetes-api/workload-resources/pod-v1/#resources).",
      "type": "object"
    },
    "global": {
      "type": "object"
    }
  },
  "title": "headmaster values",
  "type": "object"
}
